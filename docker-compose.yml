services:
  backend:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./franktech.db
    volumes:
      - .:/app
      - ./franktech.db:/app/franktech.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
